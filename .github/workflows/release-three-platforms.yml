on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - "v*"

# Existing content of the file remains unchanged here...
# Add the remaining steps as they are in the original workflow file
