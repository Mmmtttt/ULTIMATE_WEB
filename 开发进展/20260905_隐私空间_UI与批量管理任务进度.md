# 2026-09-05 隐私空间、UI 与批量管理任务进度

> 目标：以当前代码库为准逐项修复。每完成一项，记录完成情况、验证方式、注意事项。

## 任务清单

| 编号 | 任务 | 状态 | 验证 | 注意事项 |
| --- | --- | --- | --- | --- |
| 1 | 隐私模式文件保存位置独立于正常模式数据目录 | 已完成 | `python -m pytest comic_backend/tests/test_storage_space_dynamic_paths.py ...` 通过 | 已将 `core.constants` 中 JSON、封面、缓存、本地漫画、本地视频等路径改为运行时按当前空间解析；本地漫画软链接密码和导入工作区也改到当前空间缓存目录；默认 private 目录改为 normal 数据目录旁边的 `UltimateData_private`，并兼容旧默认值 |
| 2 | 恢复“最近导入”动态标签生成机制 | 已完成 | `python -m pytest comic_backend/tests/test_local_import_commit_behavior.py ...` 通过 | 本地漫画导入提交成功后会确保 `tag_recent_import/最近导入` 存在，并把本次成功写入的内容加入该标签 |
| 3 | 正常模式下可更改登录密码，明文保存即可 | 已完成 | `python -m pytest comic_backend/tests/test_auth_password_update.py ...` 与 `npm run build` 通过 | 新增 `PUT /api/v1/auth/password`；仅 normal 空间允许修改；保存到 `server_config.json` 的 `auth.password`；UI 在非 normal 空间禁用保存按钮 |
| 4 | 标签详情页添加分页，类似清单详情 | 已完成 | `npm run build` 通过 | 标签详情的本地库、预览库、全部三个 tab 分别分页，并按内容类型和标签 ID 记忆页码 |
| 5 | 本地漫画详情页添加补全信息按钮 | 已完成 | `npm run build` 通过 | 仅本地导入漫画详情页显示，复用现有补全接口 |
| 6 | 排查漫画补全信息是否会修正作者作品名称 | 已完成 | 代码阅读：`refresh_local_comic_metadata` 写作者和标签，不写标题 | 现有逻辑会更新作者、标签和本地补全标记，不会修改作品标题；如需修正标题，需要单独定义覆盖策略 |
| 7 | 标签编辑改为居中弹窗，并排查/取缔项目下拉框 | 已完成 | `rg "van-popover|dropdown|select-dropdown|van-dropdown|<select|el-select|n-dropdown|q-select" comic_frontend/src` 无命中；`npm run build` 通过 | 标签新增/编辑已改为居中弹窗；系统配置选择项、本地库/预览库更多菜单都改为弹窗/面板，不再使用下拉框 |
| 8 | 打包后根目录只保留一个 ps 和 bat 启动脚本，其余进脚本目录 | 已完成 | `python -m pytest tests/test_package_unified_pyinstaller_paths.py ...` 通过 | 打包 bundle 根目录启动入口改为 `start_project.bat`、`start_project.ps1`，辅助脚本移动到 `scripts/`；README 已同步 |
| 9 | 第三方库增加项目内启用开关，默认关闭，禁用时搜索/导入不可用 | 已完成 | `python -m pytest comic_backend/tests/test_protocol_runtime_config_bootstrap.py comic_backend/tests/test_provider_manager_enabled_guard.py ...` 通过；真实注册器确认 `video.javbus effective_config_key='javdb'` | 项目层把缺失的 `enabled` 默认视为关闭；协议执行入口、客户端创建、平台状态统一检查 enabled；未启用平台不会进入前端搜索/导入平台列表；JAVBUS 作为 JAVDB 子能力读取 `javdb.enabled`，不再拥有独立开关；通过主项目 `plugins/javbus-parent-config/ultimate-host.json` 覆盖，保证 GitHub 打包无需依赖子模块提交 |
| 10 | 构建时增加第三方库打包开关，默认全开，关闭则不打包目录 | 已完成 | `python -m pytest tests/test_package_unified_pyinstaller_paths.py tests/test_package_unified_mobile_protocol_snapshot.py` 通过；工作流 YAML 解析通过 | GitHub 手动工作流新增各第三方库打包开关；脚本支持 `--third-party-excludes`；桌面内嵌/外置与 Android 快照共用过滤 |
| 11 | 启动首屏偶发显示全部漫画无分页 | 已完成 | `npm run build` 通过 | 本地库和预览库在后端偶发返回完整数组且 total 等于数组长度时，前端兜底按当前页切片，避免首屏全量渲染 |
| 12 | 批量加入清单时清单类型未按漫画/视频区分 | 已完成 | `npm run build` 通过 | 本地库和预览库打开批量加入清单弹窗前，按当前漫画/视频模式强制刷新并过滤可选清单 |
| 13 | 清单详情页增加移出清单和移到回收站 | 已完成 | `npm run build` 通过 | 批量管理时保留“移出清单”，新增“移到回收站”，覆盖本地/预览、漫画/视频 |
| 14 | 新增漫画/视频阅读记录，第三方搜索结果不记录 | 已完成 | `python -m pytest tests/test_reading_history_app_service.py tests/test_reading_history_api.py -q` 通过；`npm run build` 通过；`python -m py_compile comic_backend\application\reading_history_app_service.py comic_backend\api\v1\history.py` 通过 | 阅读记录独立保存到 `reading_history_database.json`，按当前 normal/private 空间自动隔离；本地库和预览库详情页访问会记录，第三方平台详情页不会记录；漫画和视频各保留最近 30 条 |
| 15 | 数据同步页新增独立“局域网传输”子功能 | 已完成 | `python -m pytest tests/test_lan_transfer_app_service.py tests/test_lan_transfer_api.py -q` 通过；`python -m pytest tests/test_frontend_server_proxy_auth.py -q` 通过；`npm run build` 通过；`python -m py_compile comic_backend\application\lan_transfer_app_service.py comic_backend\api\v1\transfer.py comic_frontend\frontend_server.py` 通过 | 入口位于数据同步页，但后端为独立 `/api/v1/transfer`；支持发布文字、上传当前设备文件、登记服务器文件路径；删除服务器文件传输项不会删除服务器原文件；打包前端代理已支持 multipart 上传转发 |

## 变更记录

- 初始化任务进度文档。
- 隐私空间路径代理、最近导入标签、密码修改接口、标签详情分页、详情页补全按钮、清单详情批量移出/移到回收站已完成第一版。
- 第三方库运行时启用默认关闭；打包时新增按目录排除机制与 GitHub 工作流开关。
- 完成最终验证：目标后端测试 43 项通过；`npm run build` 通过；Python 关键文件 `py_compile` 通过；GitHub workflow YAML 解析通过；前端下拉框关键词检索无命中。
- 补充修正：JAVBUS 协议能力通过主项目 host overlay 新增父配置键 `config_parent_key=javdb`；后端使用 `effective_config_key` 读取父配置，前端平台列表也按有效配置键过滤，因此 JAVBUS 启用状态跟随 JAVDB 开关；桌面 bundled/external 与 Android 快照均会携带该覆盖文件。
- 追加阅读记录功能：新增后端轻量历史服务、API、前端历史页面和“我的”入口；四个本地/预览详情页静默写入访问记录，搜索临时详情不写入。
- 追加局域网传输功能：同步中心新增独立工具卡，后端新增轻量传输服务，上传文件落入当前空间缓存目录，服务器路径仅登记引用，下载时按传输项直接发送。
- 调整局域网传输入口：移动到操作日志下方，并改为默认折叠，展开后再加载传输列表和显示上传/发布操作。
