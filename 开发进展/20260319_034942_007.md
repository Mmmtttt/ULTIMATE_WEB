# 20260319_034942_007

## 1) Completed unit
- Added/updated Windows base-environment installer script:
  - `scripts/install_build_env_windows_base.ps1`
- Script installs and configures:
  - Python packages: `pyinstaller`, `flask-cors`
  - Java 17 (winget fallback)
  - Android SDK cmdline-tools + sdkmanager + adb
  - Android SDK packages (platform-tools, platform API, build-tools)
  - conda base activate scripts for persistent PATH/JAVA_HOME/ANDROID_SDK_ROOT

## 2) Bugfix from user execution feedback
- Fixed failure after winget Java install where `JAVA_HOME` was not available in current shell.
- Added automatic Java home resolution and explicit `JAVA_HOME` + `PATH` setup before sdkmanager license step.

## 3) Next target
- Optional: add `-SkipPython`, `-SkipJava`, `-SkipAndroidSdk` switches for faster reruns after partial install.
