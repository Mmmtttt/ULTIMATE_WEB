# 20260319_0358xx_008

## 1) Completed unit
- Fixed installer robustness for Java and conda activation script generation.
- Avoided PowerShell reserved variable collision (`$HOME`) by renaming to `$javaHomeCandidate`.
- Improved Java detection logic:
  - detect from PATH first,
  - fallback to installation directory scan,
  - winget non-zero tolerated when Java is already installed.
- Fixed activation script writer to output line-by-line content instead of single-line flattened content.
- Simplified PATH injection loop for Android tool paths in generated `android_sdk_env.ps1`.

## 2) Files touched
- scripts/install_build_env_windows_base.ps1

## 3) Next target
- Re-run installer in a no-profile PowerShell session to rewrite broken conda activate script, then proceed with APK/exe build verification.
